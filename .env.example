# Dreamcatcher Demo — environment variables
#
# This demo runs entirely on static mock fixtures with no backend and no network calls.
# No environment variables are required to run or build the demo.
