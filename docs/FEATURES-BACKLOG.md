# Features backlog — dreamcatcher-showcase

> Routing rule (Work Item Routing Standard): this file holds **capabilities that
> do not exist yet and may be built**. Defects and deferred debt live in
> `ISSUES.md`, never here. Work moves to `ROADMAP.md` once decided and sequenced.

## Candidates

- **Constellation view (deterministic, record-derived) — after Oct 1 2026.**
  A secondary Graph rendering with more visual presence than the capture-order
  line: positions still derived from the record (seeded by capture index and
  citation anchors, never runtime physics), so "same data, same layout, every
  time" holds. Origin: owner asked at T-1 (2026-09-16) for the popular 3D
  "molecule" look; declined for this demo — see `DECISIONS.md` 2026-09-16.
  Gate before it ships: axe A/AA on the rendering surface with zero exclusions,
  desk-only on phones (`DESK_ONLY_VIEWS`), the capture-order view stays default.
