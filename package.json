{
  "name": "dreamcatcher-showcase",
  "description": "Dreamcatcher — public cockpit demo. A workspace that catches scattered AI-conversation fragments and nets them into structured, traceable project notebooks, with citation-backed retro-tracing and a citation-edged relationship graph. Mock data; the engine is private.",
  "homepage": "https://garthpuckerin-dreamcatcher.vercel.app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:unit": "node --test",
    "test:sweep:mobile": "node scripts/mobile-sweep.mjs",
    "test:sweep:viewport": "node scripts/viewport-sweep.mjs",
    "test:sweep:whiteglove": "node scripts/whiteglove-sweep.mjs",
    "test:sweeps": "node scripts/run-release-sweeps.mjs",
    "test:release": "npm run build && npm run test:unit && npm run test:e2e && npm run test:sweeps"
  },
  "dependencies": {
    "lucide-react": "^0.294.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@tailwindcss/typography": "^0.5.19",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.19",
    "vite": "^5.0.8"
  }
}
